package deadman

import (
	"github.com/xiaozefeng/go-web-crawler/engine"
	"regexp"
)

var titleRe = regexp.MustCompile(``)
var bodyRe = regexp.MustCompile(``)

func ParseChapter(content []byte) engine.ParseResult {
	var r engine.ParseResult

	return r
}
